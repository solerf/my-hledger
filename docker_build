#! /usr/bin/env sh
set -eu

usage() {
  echo "usage: $0 <Dockerfile-hledger|Dockerfile-hledgerapp>" >&2
  exit 2
}

[ $# -eq 1 ] || usage

case "$1" in
  Dockerfile-hledger)    tag=hledger ;;
  Dockerfile-hledgerapp) tag=hledger-app ;;
  *) usage ;;
esac

docker build -t "$tag" -f "$1" .
